import { PaginatorStyleDirective } from './paginator-style.directive';

describe('PaginatorStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new PaginatorStyleDirective();
    expect(directive).toBeTruthy();
  });
});
